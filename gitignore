# Personal config — contains API keys, P&L data
# NEVER commit this. The default config is auto-generated by the plugin
# on first run; commit only the schema in source.
.xbar_stock_config.json
xbar_stock_config.json
*.config.json

# Runtime state (per-user, ephemeral)
/tmp/xbar_stock*
.DS_Store

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/

# Editors
.vscode/
.idea/
*.swp
*~

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Anything that smells like a secret leaked in
*.key
*.pem
secrets.*
credentials.*
